{
  "name": "distributed-energy-trading-demo",
  "version": "0.1.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy-local": "npx hardhat run --network localhost scripts/deploy.js"
  },
  "devDependencies": {
    "hardhat": "^2.14.0",
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "ethers": "^6.0.0"
  }
}